apiVersion: v1
kind: Pod
metadata:
  name: lstm
spec:
  securityContext:
    runAsUser: 270999
    runAsGroup: 1132
  restartPolicy: Never
  containers:
    - name: lstm-pod
      #image: docker.io/pytorch/pytorch:2.5.1-cuda12.4-cudnn9-runtime
      image: ghcr.io/stmorse/torch-gen:latest
      resources:
        requests:
          memory: '64Gi'
          nvidia.com/gpu: '1'
          cpu: '16'
        limits:
          memory: '64Gi'
          nvidia.com/gpu: '1'
          cpu: '16'
      command: ['/bin/sh']
      args:
        - '-c'
        - |
          echo "Running container..."   
          cd /sciclone/home/stmorse/classes/nndl/project
          python -u lstm_baseline.py \
            --subfolder charged --suffix _charged5 \
            --save-folder logs/lstm --epochs 50 \
            > logs/lstm/lstm_charged_25-05-01.log 2>&1
          # sleep infinity
      volumeMounts:
        - name: home
          mountPath: /sciclone/home/stmorse
  volumes:
    - name: home
      nfs:
        server: 128.239.56.166
        path: /sciclone/home/stmorse